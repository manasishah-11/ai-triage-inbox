[build]
  command = "npm run build"
  publish = "dist"

# Client-side routing (e.g. /inbox, /inbox/:id). Static files in dist are still served first.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
